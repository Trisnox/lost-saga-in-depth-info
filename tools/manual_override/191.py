# Maximum combo is mismatched
def overwrite(key, hint):
    if key == 'jump_max_combo':
        return 4
    # Counter exist, but has no attribute
    elif key == 'enable_counter_attack':
        return 0