# This script will be run for every valid mercenaries. You won't likely using this.
# This script will also be run first, then the individual mercenary if any, and finally the default keys.
# The main problem is that this run on all mercenaries, this means that it'll prone to errors